# config.py
# ─────────────────────────────────────────────────────────────────────────────
# B:in 프로젝트 전체 설정값을 한 곳에서 관리합니다.
# 모델 경로, 클래스명, 임계값 등을 여기서 수정하면 모든 파일에 반영됩니다.
# ─────────────────────────────────────────────────────────────────────────────

from pathlib import Path

# ── 프로젝트 루트 경로 ────────────────────────────────────────────────────────
ROOT = Path(__file__).parent

# ── 모델 경로 ─────────────────────────────────────────────────────────────────
MODEL_PT   = ROOT / "models" / "waste_detector.pt"      # PyTorch 가중치
MODEL_ONNX = ROOT / "models" / "waste_detector.onnx"    # ONNX 변환 결과

# ── 데이터셋 경로 ─────────────────────────────────────────────────────────────
DATA_YAML  = ROOT / "data" / "dataset.yaml"

# ── 출력 경로 ─────────────────────────────────────────────────────────────────
OUTPUT_DIR = ROOT / "outputs"

# ── 1차 분류 클래스 (학습 순서와 동일하게 유지) ───────────────────────────────
CLASS_NAMES = ["pet_bottle", "vinyl", "can", "paper"]

# 한국어 표시명
CLASS_NAMES_KO = {
    "pet_bottle": "페트병",
    "vinyl":      "비닐",
    "can":        "캔",
    "paper":      "종이",
}

# ── 2차 분류 클래스 (페트병 세부 분류) ───────────────────────────────────────
SUB_CLASS_NAMES = ["transparent_plastic", "colored_plastic"]

SUB_CLASS_NAMES_KO = {
    "transparent_plastic": "무색 플라스틱",
    "colored_plastic":     "유색 플라스틱",
}

# ── 추론 파라미터 ─────────────────────────────────────────────────────────────
CONF_THRESHOLD = 0.25   # Confidence 임계값 (이 값 미만은 무시)
IOU_THRESHOLD  = 0.50   # NMS IoU 임계값
IMG_SIZE       = 640    # 모델 입력 이미지 크기 (픽셀)

# ── 학습 파라미터 ─────────────────────────────────────────────────────────────
TRAIN_EPOCHS  = 100
TRAIN_BATCH   = 16
TRAIN_WORKERS = 4
PRETRAINED    = "yolov8n.pt"   # Ultralytics 사전학습 모델

# ── 시각화 색상 (BGR 형식) ────────────────────────────────────────────────────
# 클래스별 바운딩 박스 색상
CLASS_COLORS = {
    "pet_bottle": (0,   200,  50),   # 초록
    "vinyl":      (255, 165,   0),   # 주황
    "can":        (0,   120, 255),   # 파랑
    "paper":      (180,   0, 255),   # 보라
}

DEFAULT_COLOR = (200, 200, 200)      # 알 수 없는 클래스용 회색

# ── 분리배출 가이드 ───────────────────────────────────────────────────────────
DISPOSAL_GUIDE = {
    "pet_bottle": "라벨을 제거하고 내용물을 비운 뒤, 압착하여 투명 페트병 전용 수거함에 배출하세요.",
    "vinyl":      "이물질(음식물 등)을 제거한 후 비닐류 전용 수거함에 배출하세요.",
    "can":        "내용물을 완전히 비우고 캔 수거함에 배출하세요. (부탄캔은 구멍을 뚫어 잔가스 제거)",
    "paper":      "물기와 이물질을 제거한 후 종이류 수거함에 배출하세요. (코팅지·영수증 제외)",
    # 2차 분류 세부 안내
    "transparent_plastic": "무색 페트병 전용 수거함에 라벨 제거 후 압착 배출",
    "colored_plastic":     "유색 플라스틱 수거함에 배출 (재활용 가치 낮음, 최대한 무색 사용 권장)",
}

# ── Video Smoothing 파라미터 ──────────────────────────────────────────────────
SMOOTH_WINDOW = 5   # Moving Average 윈도우 크기 (프레임 수)